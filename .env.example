# Optional. The app works without these by using its private local study coach.
# Keep real values in .env, which is ignored by Git.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra

# Optional absolute path for the personal-PC SQLite database.
# Defaults to ./data/homework-helper.sqlite
HOMEWORK_HELPER_DB_PATH=
